{
  "name": "jtnextgen-portal",
  "version": "1.0.0",
  "private": true,
  "description": "JT NextGen Tech Hub portal — backend API + React frontend",
  "scripts": {
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend install",
    "start": "npm --prefix backend start",
    "start:prod": "npm --prefix backend run start:prod",
    "test": "npm --prefix backend test",
    "dev": "concurrently -n API,WEB -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}